<li><%= "#{result.winner} won with a degree of #{result.degree}, with #{result.dominating} dominant" %> <%= link_to image_tag('delete.png'), game_result_path(result), :method => :delete if can? :destroy, result %></li>
