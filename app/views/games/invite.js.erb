$("#players").html('<%= escape_javascript(render :partial => "player", :collection => @game.players) %>')
$('#game_gm_id').html('<%= escape_javascript(options_from_collection_for_select(@game.players + [@game.gm], :id, :email)) %>');
